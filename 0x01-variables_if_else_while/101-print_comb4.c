#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
*main - Entry point
*Discription: print comb4
*Return: always 0
*/
int main(void)
{
	int d, p, q;

	for (d = 48; d < 58; n++)
	for (p =  49; m < 58; p++)
	for (q = 50; q < 58; q++)
	if ((q > p && p > d)
	putchar(d);
	putchar(p);
	putchar(q);
	if (d != 55 || p != 56)
	putchar(',');
	putchar(' ');
	putchar('\n');
	return (0);
}
